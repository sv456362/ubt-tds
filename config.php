<?php
/**
 * Конфигурация. Правьте только этот файл
 * (логин/пароль меняются через tools/make_hash.php).
 */

// Логин администратора.
// По умолчанию admin (пока не задан хэш пароля ниже).
define('ADMIN_LOGIN', 'admin');

// Хэш пароля администратора.
// ПОКА СТРОКА ПУСТАЯ — режим первого запуска:
//   - вход admin:admin (форма входа предзаполнена),
//   - в админке висит жирное красное предупреждение со ссылкой на tools/make_hash.php.
// Как только хэш записан (кнопкой в tools/make_hash.php или вручную) —
// дефолтный пароль отключается автоматически.
define('ADMIN_PASSWORD_HASH', '');

// Секретный ключ для CSRF-токенов, сессии и хэширования IP. Смените на свою случайную строку.
define('APP_SECRET', 'mlkvkfrvmerkvmerlkc'.'0000');

/**
 * Имя файла панели администратора.
 * Для защиты от перебора переименуйте admin.php во что-то непредсказуемое,
 * например secretpanel8f3k2.php, и укажите это же имя здесь —
 * все ссылки и формы внутри панели будут собираться по этому значению.
 * Если имя файла и константа разойдутся, панель сама предложит
 * кнопку «Записать <имя> в config.php».
 */
define('ADMIN_PANEL_FILE', 'admin.php');

/**
 * Тип HTTP-редиректа по умолчанию при переходе по ссылке кампании.
 * 302 — временный (рекомендуется, поисковики не запоминают целевой URL навсегда).
 * 301 — постоянный (браузеры и краулеры могут закэшировать редирект надолго).
 * Можно переопределить индивидуально для каждой кампании в панели управления.
 */
define('DEFAULT_REDIRECT_TYPE', 302);

/**
 * Куда отправлять посетителя, если ссылка не найдена, кампания выключена
 * или у неё вообще нет офферов. Оставьте пустой строкой, чтобы просто
 * показывать страницу 404 без редиректа.
 */
define('FALLBACK_REDIRECT_URL', 'https://google.com');

// Пути к файлам-хранилищам (не трогать, если не знаете зачем)
define('DATA_DIR', __DIR__ . '/data');
define('LINKS_FILE', DATA_DIR . '/links.dat.php');
define('STATS_FILE', DATA_DIR . '/stats.dat.php');

// Имя сессионной cookie
define('SESSION_NAME', 'tds_admin_sess');

// Часовой пояс для дат в статистике
date_default_timezone_set('Europe/Moscow');